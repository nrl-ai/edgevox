# Robot tool-calling benchmark

Multi-model comparison of LLM tool-call emissions across four robot agent surfaces (scout, irsim, panda, humanoid). Generated by `scripts/bench_robot_tool_calling.py`.

Run config: temperature=0.1 · max_tokens=128 · n_ctx=4096 · scenarios=10

## Scoreboard

| Model | Avg score | Tool-call rate | Load (s) | Per-reply (s) | Errors |
|---|---|---|---|---|---|
| `qwen2.5-1.5b` | **90.0** | 89% | 5.3 | 2.43 | — |

## Per-agent average

| Model | scout | irsim | panda | humanoid |
|---|---|---|---|---|
| `qwen2.5-1.5b` | 90 | — | — | — |

## Per-scenario scores

| Model | scout/nav_kitchen | scout/nav_bedroom | scout/light_on_kitchen | scout/light_off_bedroom | scout/query_battery | scout/query_current_room | scout/list_rooms | scout/return_home | scout/light_check_kitchen | scout/conversational |
|---|---|---|---|---|---|---|---|---|---|---|
| `qwen2.5-1.5b` | 100 | 100 | 100 | 100 | 100 | 100 | 0 | 100 | 100 | 100 |

## Sample predictions

### `scout/nav_kitchen` — 'Go to the kitchen.'

*Expected:* `navigate_to_room({'room': 'kitchen'})`

- **`qwen2.5-1.5b`** → `navigate_to_room({'room': 'kitchen'})` · score 100

### `scout/nav_bedroom` — 'Drive to the bedroom please.'

*Expected:* `navigate_to_room({'room': 'bedroom'})`

- **`qwen2.5-1.5b`** → `navigate_to_room({'room': 'bedroom'})` · score 100

### `scout/light_on_kitchen` — 'Turn on the kitchen light.'

*Expected:* `turn_on_light({'room': 'kitchen'})`

- **`qwen2.5-1.5b`** → `turn_on_light({'room': 'kitchen'})` · score 100

### `scout/light_off_bedroom` — 'Turn off the bedroom lights.'

*Expected:* `turn_off_light({'room': 'bedroom'})`

- **`qwen2.5-1.5b`** → `turn_off_light({'room': 'bedroom'})` · score 100

### `scout/query_battery` — "What's your battery level?"

*Expected:* `battery_level({})`

- **`qwen2.5-1.5b`** → `battery_level({})` · score 100

### `scout/query_current_room` — 'Which room are you in right now?'

*Expected:* `current_room({})`

- **`qwen2.5-1.5b`** → `current_room({})` · score 100

### `scout/list_rooms` — 'What rooms do you know about?'

*Expected:* `list_rooms({})`

- **`qwen2.5-1.5b`** → `(no tool call)` · score 0  ⚠ no tool call emitted

### `scout/return_home` — 'Come back home.'

*Expected:* `return_home({})`

- **`qwen2.5-1.5b`** → `return_home({})` · score 100

### `scout/light_check_kitchen` — 'Is the kitchen light on?'

*Expected:* `light_state({'room': 'kitchen'})`

- **`qwen2.5-1.5b`** → `light_state({'room': 'kitchen'})` · score 100

### `scout/conversational` — "Hi Scout, how's it going?"

*Expected:* (no tool call)

- **`qwen2.5-1.5b`** → `(no tool call)` · score 100

## Methodology

Each scenario is a natural-language utterance the voice pipeline would hand the agent, paired with the expected tool call. The full agent persona + OpenAI-format tool schemas are fed to the LLM via `LLM.complete(messages, tools=...)`. When the binding doesn't hoist structured `tool_calls`, EdgeVox's post-hoc parser chain runs against the raw content (same chain the agent loop uses).

Scoring: 100 for exact tool + exact args, 90 for an accepted alias, 60-89 for right tool with some-correct args, 25 for wrong tool, 40 for a spurious call, 0 for no call when one was expected.

## See also

- [`chess-commentary-benchmark.md`](./chess-commentary-benchmark) — commentary-quality benchmark on the same model pool.
- [`slm-tool-calling-benchmark.md`](./slm-tool-calling-benchmark) — single-tool detection smoke test.
